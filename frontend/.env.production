# Production build config for the standalone Vercel (frontend) + Render
# (backend) deployment - see frontend/.env.example for what this does.
# Not a secret: this is just the public URL of the backend API.
VITE_API_BASE_URL=https://rockstar-organics-api.onrender.com
